# Gap-To-Target Plan — {{Company}}

> Two jobs, depending on the verdict:
>  • on a **GO** — the de-risk items to fix BEFORE / EARLY IN the engagement, so the build doesn't stall.
>  • on a **NOT-YET** — the clearance items the company must fix to flip a failed gate and re-qualify.
> Each item is a concrete action with an owner and a "done =" — no vague advice. Nothing here is the
> technical fix itself (that's E2); these are the readiness conditions the build depends on.

**Verdict:** {{GO | NOT-YET}}  ·  **Index:** {{TOTAL}}/100 ({{tier}})  ·  **Date:** {{date}}

---

## Gaps (lowest-scoring / gate-failing dimensions first)

### Gap 1 — {{Dimension}} (Level {{L}}{{, gate {Gx} FAIL}})
- **The gap:** {{what's missing, in one line}}
- **Why it matters:** {{what it blocks or risks — e.g., "without a single quote-data source, a pilot can't read the inputs"}}
- **Fix:** {{the concrete action — e.g., "consolidate quote data into HubSpot custom object"}}
- **Owner:** {{who on the client side}}
- **When:** {{before kickoff | first 2 weeks of Phase 1 | before re-qualify}}
- **Done =:** {{the observable condition — e.g., "all live quotes readable from one source via API"}}
- **In-engagement?** {{yes — first move in Phase 1 | no — must be cleared before we start}}

### Gap 2 — {{Dimension}} (Level {{L}})
- **The gap:** {{...}}
- **Why it matters:** {{...}}
- **Fix:** {{...}}
- **Owner:** {{...}}
- **When:** {{...}}
- **Done =:** {{...}}
- **In-engagement?** {{...}}

### Gap 3 — {{Dimension}} (Level {{L}})
- {{...}}

---

## Technical risks to validate in E2 (flagged, NOT solved here)

> These are NOT gaps the client fixes — they're risks the architect (caio-enterprise-workflow-architect)
> must validate on real access in Phase 2. Listed so the SOW carries them as known unknowns.

- {{e.g., "the {tool} SOAP API is reportedly fragile — E2 must confirm it exposes {fields} before committing the beachhead build"}}
- {{e.g., "data-residency for {region} — E2 confirms the hosting/model constraints"}}

---

## On a NOT-YET — the re-qualify trigger

```
Re-qualify when ALL of these are true:
  • {{gate fix 1 — e.g., "a budget-holding sponsor is named"}}
  • {{gate fix 2 — e.g., "an API/integration path on {tool} is confirmed"}}
  • {{gate fix 3 — e.g., "one painful weekly workflow is named"}}
Target window: {{e.g., 60 days}}. Then re-run caio-ai-readiness-assessment (re-qualification mode).
```

---
*Generated by caio-ai-readiness-assessment v1.0.0 · {{date}}. Every item has an owner and a "done =".
Readiness conditions only — the technical build is the engagement's job.*
