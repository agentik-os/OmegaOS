# Operator context for Storyteller {OS} — DEFAULT TEMPLATE

Use this as a default for operator-facing work. Update it when the user says
otherwise. Never expose it to another identified user. This is a GENERIC
template shipped with OmegaOS: the user's real, private context lives at
`~/.omega/os/storytelling-os/ledger/context.md` (user-local, never committed) —
the master agent reads that first when present, and falls back to this template.

## Strategic world

- <who you are: role, what you build, what you sell>.
- <your active narratives / themes you keep returning to>.
- <your ventures — keep them as DISTINCT story domains, never merged>.
- <your audiences and the channels you show up on>.

## Voice

- <how you actually sound: rhythm, vocabulary, what you never say>.
- <tone range: when you go raw vs polished>.
- Preserve the operator's voice above all — never flatten it into generic copy.

## Evidence + truth standard

- <what "true" means for your stories: lived / documented / composite-labeled>.
- Never fabricate a lived detail; label reconstruction and composite honestly.
- Consent: never expose a third party's private story without a consent record.

## Jobs the stories must do

- <what each story is FOR: trust, teaching, selling, positioning, connection>.
